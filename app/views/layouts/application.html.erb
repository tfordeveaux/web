<!DOCTYPE html>
<html>
<head>
  <title>Aperoinsa</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href="favicon.ico" rel="icon" type="image/x-icon" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
   <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="<%= root_url %>">Insapéro</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <li class="active"><a href="<%= aperos_path %>">Events</a></li>
              <li><a href="<%= new_apero_path %>">Créer un apéro</a></li>
              <li> <%= link_to "Aide-Apéro", aide_path %></li>
              <li> <%= link_to "Contact", contact_path %></li>
              <li> <%= link_to "About us", about_path%></li>

            </ul>
            <form class="navbar-form pull-right">
              <% if user_signed_in? %>
               Signed in as <%= current_user.email %>. 
              <%= link_to "Log out", destroy_user_session_path %>
              <% else %>
              <%= link_to "S'enregistrer", new_user_registration_path %> or <%= link_to "Se connecter", new_user_session_path %>
              <% end %>
              </ul>
            </form>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>

   <div class="container">
    <div class="row">
        <div class="span12"><%= yield %>
        </div>
    </div>
  </div>


</body >

</html>
